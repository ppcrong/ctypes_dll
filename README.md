# ctypes_dll
dll for python ctypes test

*CMake build steps*

**First build**
```sh
mkdir build
cd build
rd CMakeFiles -force -recurse;cmake ..;cmake --build .
```

**Build**
```sh
cd build
rd CMakeFiles -force -recurse;cmake ..;cmake --build .
```